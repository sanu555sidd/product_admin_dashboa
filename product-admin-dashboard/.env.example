# API base URL (default: https://dummyjson.com)
NEXT_PUBLIC_API_URL=https://dummyjson.com

# Optional: artificial latency in ms added to list/detail requests (?delay=).
# Set to 2000 to test the "fast typing / stale response" case.
NEXT_PUBLIC_API_DELAY=
